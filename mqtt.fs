BEGIN-STRUCTURE mqtt-server
   CFIELD: ->reconnect
    FIELD: ->host-length
    FIELD: ->host-addr
    FIELD: ->host-port
    FIELD: ->user-id-length
    FIELD: ->user-id
    FIELD: ->keep-alive
END-STRUCTURE

